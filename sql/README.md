# SQL Scripts

This directory contains SQL scripts used to create and manage the database structure.

Subfolders will contain:

schema → database schemas
tables → table creation scripts
views → analytical views
